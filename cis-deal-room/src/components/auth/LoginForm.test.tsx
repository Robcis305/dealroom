import { describe, it, expect } from 'vitest';

// Wave 0 stubs — these tests must FAIL (RED) until implemented in Plan 01-02.
describe('LoginForm', () => {
  it('renders an email input and submit button', () => {
    // TODO: render <LoginForm />, assert email input and submit button present
    expect(true).toBe(false);
  });

  it('transitions to confirmation state after form submission', () => {
    // TODO: fill email, submit, assert confirmation message with email address shown
    expect(true).toBe(false);
  });

  it('shows the submitted email in the confirmation message', () => {
    // TODO: fill email, submit, assert email address visible in confirmation text
    expect(true).toBe(false);
  });
});
