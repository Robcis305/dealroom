---
phase: 01-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - cis-deal-room/package.json
  - cis-deal-room/src/db/schema.ts
  - cis-deal-room/src/db/index.ts
  - cis-deal-room/src/lib/auth/tokens.ts
  - cis-deal-room/src/lib/auth/session.ts
  - cis-deal-room/src/lib/auth/rate-limit.ts
  - cis-deal-room/src/types/index.ts
  - cis-deal-room/vitest.config.ts
  - cis-deal-room/src/test/setup.ts
  - cis-deal-room/src/lib/auth/tokens.test.ts
  - cis-deal-room/src/lib/auth/session.test.ts
  - cis-deal-room/src/lib/auth/rate-limit.test.ts
  - cis-deal-room/src/lib/dal/workspaces.test.ts
  - cis-deal-room/src/lib/dal/folders.test.ts
  - cis-deal-room/src/lib/dal/activity.test.ts
  - cis-deal-room/src/components/auth/LoginForm.test.tsx
  - cis-deal-room/src/app/auth/verify/page.test.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-06

must_haves:
  truths:
    - "Next.js 15 project exists and compiles with TypeScript strict mode"
    - "Database schema defines all Phase 1 tables with correct column types and relations"
    - "generateToken() produces 64-char hex string; hashToken() produces deterministic SHA-256 hex"
    - "createSession() inserts a sessions row; getSession() validates the 24h sliding window"
    - "authSendLimiter and authVerifyLimiter instances are exported and use Upstash Redis"
    - "All test stubs exist and fail (RED state) — Wave 0 complete"
  artifacts:
    - path: "cis-deal-room/src/db/schema.ts"
      provides: "All Drizzle table definitions for Phase 1"
      exports:
        - users
        - sessions
        - magicLinkTokens
        - workspaces
        - folders
        - folderAccess
        - workspaceParticipants
        - activityLogs
    - path: "cis-deal-room/src/lib/auth/tokens.ts"
      provides: "Token generation and hashing utilities"
      exports: ["generateToken", "hashToken", "timingSafeTokenCompare"]
    - path: "cis-deal-room/src/lib/auth/session.ts"
      provides: "Database session CRUD"
      exports: ["createSession", "getSession", "destroySession", "setSessionCookie"]
    - path: "cis-deal-room/src/lib/auth/rate-limit.ts"
      provides: "Upstash rate limiter instances"
      exports: ["authSendLimiter", "authVerifyLimiter"]
    - path: "cis-deal-room/src/types/index.ts"
      provides: "Shared TypeScript types"
      exports: ["Session", "WorkspaceStatus", "CisAdvisorySide", "ParticipantRole"]
  key_links:
    - from: "cis-deal-room/src/lib/auth/session.ts"
      to: "cis-deal-room/src/db/schema.ts"
      via: "Drizzle query on sessions table"
      pattern: "db\\..*sessions"
    - from: "cis-deal-room/src/lib/auth/tokens.ts"
      to: "Node.js crypto"
      via: "crypto.createHash('sha256') and crypto.randomBytes"
      pattern: "createHash|randomBytes"
---

<objective>
Bootstrap the CIS Deal Room Next.js project, define the complete Phase 1 database schema, implement auth primitives (token generation/hashing, database session management, rate limiting), and scaffold all test stubs to RED state (Wave 0).

Purpose: Every subsequent plan inherits the schema types and auth utilities from this plan. No Phase 1 work can proceed without the DB schema and lib/auth contracts being established first.
Output: Compiling Next.js app, schema.ts with 8 tables, three auth lib files, Vitest config, 8 test stub files in failing RED state.
</objective>

<execution_context>
@/Users/robertlevin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robertlevin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bootstrap Next.js project and define complete database schema</name>
  <files>
    cis-deal-room/ (entire project scaffold),
    cis-deal-room/src/db/schema.ts,
    cis-deal-room/src/db/index.ts,
    cis-deal-room/src/types/index.ts,
    cis-deal-room/drizzle.config.ts,
    cis-deal-room/.env.example
  </files>
  <behavior>
    - schema exports: users, sessions, magicLinkTokens, workspaces, folders, folderAccess, workspaceParticipants, activityLogs
    - workspaceStatusEnum values: 'engagement' | 'active_dd' | 'ioi_stage' | 'closing' | 'closed' | 'archived'
    - cisAdvisorySideEnum values: 'buyer_side' | 'seller_side'
    - participantRoleEnum values: 'admin' | 'cis_team' | 'client' | 'counsel' | 'buyer_rep' | 'seller_rep' | 'view_only'
    - activityActionEnum values: 'uploaded' | 'downloaded' | 'viewed' | 'deleted' | 'invited' | 'removed' | 'created_folder' | 'renamed_folder' | 'created_workspace' | 'revoked_access' | 'status_changed'
    - All tables use uuid().primaryKey().defaultRandom() — never text('id') for UUID PKs
    - folders table has: id, workspaceId (FK → workspaces), name, sortOrder (integer), createdAt, updatedAt
    - folderAccess table has: id, folderId (FK → folders), participantId (FK → workspaceParticipants), grantedAt — schema ready for Phase 3 population
    - activityLogs table has: id, workspaceId, userId, action (enum), targetType (enum), targetId (uuid nullable), metadata (jsonb nullable), createdAt — no updatedAt (append-only, immutable)
    - workspaceParticipants has: id, workspaceId, userId, role (enum), status ('invited'|'active'|'revoked'), invitedAt, activatedAt nullable
    - Shared types in src/types/index.ts: Session, WorkspaceStatus (from enum values), CisAdvisorySide, ParticipantRole, ActivityAction
  </behavior>
  <action>
    Run the create-next-app scaffold from the project root (parent of where cis-deal-room/ should live — confirm it doesn't already exist):

    ```
    npx create-next-app@latest cis-deal-room --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --no-git
    ```

    Then install all Phase 1 dependencies:
    ```
    cd cis-deal-room
    npm install drizzle-orm @neondatabase/serverless iron-session resend lucide-react clsx tailwind-merge zod @upstash/ratelimit @upstash/redis @react-email/components
    npm install -D drizzle-kit dotenv tsx vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/jest-dom @types/node
    ```

    Create drizzle.config.ts:
    ```typescript
    import type { Config } from 'drizzle-kit';
    export default {
      schema: './src/db/schema.ts',
      out: './src/db/migrations',
      dialect: 'postgresql',
      dbCredentials: { url: process.env.DATABASE_URL! },
    } satisfies Config;
    ```

    Create src/db/index.ts — Drizzle singleton using @neondatabase/serverless:
    ```typescript
    import { neon } from '@neondatabase/serverless';
    import { drizzle } from 'drizzle-orm/neon-http';
    import * as schema from './schema';

    const sql = neon(process.env.DATABASE_URL!);
    export const db = drizzle(sql, { schema });
    ```

    Create src/db/schema.ts with all 8 tables (users, sessions, magicLinkTokens, workspaces, workspaceParticipants, folders, folderAccess, activityLogs). Use the exact Drizzle schema from the RESEARCH.md Code Examples section — extend it to add the folderAccess table. Every UUID column uses uuid().primaryKey().defaultRandom(). activityLogs has NO updatedAt (append-only immutability contract).

    Create .env.example listing required env vars:
    DATABASE_URL, UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN, IRON_SESSION_SECRET (min 32 chars), RESEND_API_KEY, NEXT_PUBLIC_APP_URL

    Create src/types/index.ts with TypeScript types derived from the Drizzle schema enums and table shapes.

    Do NOT port inline styles from cis-deal-portal-prototype.jsx — that prototype is reference only.
  </action>
  <verify>
    <automated>cd cis-deal-room && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `cis-deal-room/` directory exists with valid Next.js 15 project structure
    - `npm run build` or `npx tsc --noEmit` exits 0 (no type errors)
    - src/db/schema.ts exports all 8 tables with correct Drizzle types
    - src/types/index.ts exports Session, WorkspaceStatus, CisAdvisorySide, ParticipantRole, ActivityAction
    - .env.example documents all required environment variables
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement auth primitives and scaffold Wave 0 test stubs to RED</name>
  <files>
    cis-deal-room/src/lib/auth/tokens.ts,
    cis-deal-room/src/lib/auth/session.ts,
    cis-deal-room/src/lib/auth/rate-limit.ts,
    cis-deal-room/vitest.config.ts,
    cis-deal-room/src/test/setup.ts,
    cis-deal-room/src/lib/auth/tokens.test.ts,
    cis-deal-room/src/lib/auth/session.test.ts,
    cis-deal-room/src/lib/auth/rate-limit.test.ts,
    cis-deal-room/src/lib/dal/workspaces.test.ts,
    cis-deal-room/src/lib/dal/folders.test.ts,
    cis-deal-room/src/lib/dal/activity.test.ts,
    cis-deal-room/src/components/auth/LoginForm.test.tsx,
    cis-deal-room/src/app/auth/verify/page.test.tsx
  </files>
  <behavior>
    - generateToken() returns 64-char hex string (crypto.randomBytes(32).toString('hex'))
    - hashToken(token: string) returns SHA-256 hex digest (deterministic)
    - timingSafeTokenCompare(a: string, b: string) uses crypto.timingSafeEqual — prevents timing attacks
    - createSession(userId) inserts sessions row, returns sessionId (UUID)
    - getSession(sessionId) returns Session object if row exists AND lastActiveAt > now - 24h; also slides lastActiveAt = NOW()
    - getSession(sessionId) returns null if expired or not found
    - destroySession(sessionId) deletes the sessions row
    - authSendLimiter: 5 requests per email per 15 minutes (slidingWindow)
    - authVerifyLimiter: 10 requests per IP per 15 minutes (slidingWindow)
    - Test stubs: each test file has at minimum one describe block with one it() that calls expect(true).toBe(false) — these MUST fail (RED) to prove the test scaffold is wired correctly
  </behavior>
  <action>
    Create vitest.config.ts at cis-deal-room root:
    ```typescript
    import { defineConfig } from 'vitest/config';
    import react from '@vitejs/plugin-react';
    import path from 'path';

    export default defineConfig({
      plugins: [react()],
      test: {
        environment: 'jsdom',
        setupFiles: ['./src/test/setup.ts'],
        globals: true,
      },
      resolve: {
        alias: {
          '@': path.resolve(__dirname, './src'),
        },
      },
    });
    ```

    Create src/test/setup.ts:
    ```typescript
    import '@testing-library/jest-dom';
    // Mock the DB module — tests should not make real DB calls
    vi.mock('@/db', () => ({ db: {} }));
    // Mock iron-session
    vi.mock('iron-session', () => ({
      getIronSession: vi.fn(),
    }));
    ```

    Create src/lib/auth/tokens.ts with generateToken(), hashToken(), timingSafeTokenCompare() using Node.js crypto. Use crypto.timingSafeEqual(Buffer.from(a, 'hex'), Buffer.from(b, 'hex')) for comparison.

    Create src/lib/auth/session.ts with createSession(), getSession(), destroySession(), setSessionCookie(). getSession() must UPDATE lastActiveAt on every valid access (sliding window). Use iron-session with cookie name 'cis_session' and password from process.env.IRON_SESSION_SECRET.

    Create src/lib/auth/rate-limit.ts with authSendLimiter (5 reqs/email/15min) and authVerifyLimiter (10 reqs/IP/15min) using @upstash/ratelimit + @upstash/redis. The Redis instance reads from process.env.UPSTASH_REDIS_REST_URL and UPSTASH_REDIS_REST_TOKEN.

    Create all test stub files. Each stub imports the module under test and contains at least one failing assertion:
    - tokens.test.ts: tests for generateToken length, hashToken determinism, timingSafeTokenCompare (stub with expect(true).toBe(false) placeholders)
    - session.test.ts: stubs for session creation, sliding window, expiry
    - rate-limit.test.ts: stubs asserting limiter config (mock Upstash)
    - workspaces.test.ts, folders.test.ts, activity.test.ts: stubs for DAL functions
    - LoginForm.test.tsx, verify/page.test.tsx: component render stubs

    Run the test suite and confirm it outputs RED (tests fail as expected for Wave 0):
    ```
    cd cis-deal-room && npx vitest run --reporter=verbose 2>&1 | tail -20
    ```
  </action>
  <verify>
    <automated>cd cis-deal-room && npx vitest run --reporter=verbose 2>&1 | grep -E "FAIL|failed|Tests" | tail -10</automated>
  </verify>
  <done>
    - src/lib/auth/tokens.ts, session.ts, rate-limit.ts all exist and TypeScript-clean
    - vitest.config.ts configured with jsdom + @testing-library/jest-dom setup
    - All 8 test stub files exist under their correct paths from VALIDATION.md
    - `npx vitest run` exits with failures (RED state confirmed — stubs intentionally fail)
    - No import errors in test files (modules resolve correctly)
  </done>
</task>

</tasks>

<verification>
- `cd cis-deal-room && npx tsc --noEmit` exits 0
- `npx vitest run --reporter=verbose` runs all 8+ test files and shows failures (Wave 0 RED confirmed)
- src/db/schema.ts: all 8 tables present, no raw text('id') for UUID PKs
- src/lib/auth/tokens.ts: generateToken, hashToken, timingSafeTokenCompare exported
- src/lib/auth/session.ts: createSession, getSession, destroySession, setSessionCookie exported
- src/lib/auth/rate-limit.ts: authSendLimiter, authVerifyLimiter exported
</verification>

<success_criteria>
- Next.js 15 + TypeScript app compiles with no errors
- All 8 Drizzle table definitions in schema.ts with correct column types, enums, and foreign keys
- Auth lib functions implement correct contract (token generation, SHA-256 hashing with timing-safe compare, 24h sliding session window)
- Wave 0 complete: 8 test stub files wired and failing RED in Vitest
- .env.example documents all secrets needed for the project
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-foundation/01-01-SUMMARY.md` with:
- Files created (with brief description of each)
- Key decisions made (session cookie name, iron-session config, enum values confirmed)
- Schema snapshot: table names and primary key patterns
- Test stub file paths confirmed
- Any deviations from the plan
</output>
