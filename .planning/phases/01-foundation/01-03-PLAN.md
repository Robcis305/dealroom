---
phase: 01-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - cis-deal-room/src/app/globals.css
  - cis-deal-room/src/app/layout.tsx
  - cis-deal-room/src/app/(auth)/login/page.tsx
  - cis-deal-room/src/app/auth/verify/page.tsx
  - cis-deal-room/src/components/ui/Button.tsx
  - cis-deal-room/src/components/ui/Input.tsx
  - cis-deal-room/src/components/ui/Modal.tsx
  - cis-deal-room/src/components/ui/Badge.tsx
  - cis-deal-room/src/components/auth/LoginForm.tsx
autonomous: true
requirements:
  - UI-02
  - UI-05

must_haves:
  truths:
    - "Login screen shows CIS Partners branding, email input, and transforms inline to 'Check your email' state after submit — no page navigation"
    - "/auth/verify page shows distinct inline errors for 'expired' vs 'already used' with a 'Request new link' button"
    - "CIS brand tokens applied: #E10600 accent, #0D0D0D/#141414 surfaces, DM Sans font, JetBrains Mono for data values"
    - "No emoji anywhere in the UI — lucide-react icons for all iconography"
  artifacts:
    - path: "cis-deal-room/src/app/globals.css"
      provides: "Tailwind v4 @theme config with CIS brand tokens"
      contains: "@theme"
    - path: "cis-deal-room/src/components/auth/LoginForm.tsx"
      provides: "Email input + inline confirmation state machine"
      exports: ["LoginForm"]
    - path: "cis-deal-room/src/components/ui/Button.tsx"
      provides: "Primary and ghost button variants"
      exports: ["Button"]
    - path: "cis-deal-room/src/components/ui/Modal.tsx"
      provides: "Centered overlay modal with backdrop"
      exports: ["Modal"]
    - path: "cis-deal-room/src/components/ui/Badge.tsx"
      provides: "Status badge mapping workspace lifecycle enum values"
      exports: ["Badge"]
  key_links:
    - from: "cis-deal-room/src/components/auth/LoginForm.tsx"
      to: "cis-deal-room/src/app/api/auth/send/route.ts"
      via: "fetch POST /api/auth/send on form submit"
      pattern: "fetch.*api/auth/send"
---

<objective>
Build the Tailwind v4 brand configuration, primitive UI components, and auth screens — the visual foundation that Plan 01-04 (workspace shell) builds on top of.

Purpose: Separating brand/auth from the workspace shell keeps each plan under the 15-file limit and lets primitive components be fully tested before the workspace shell uses them. Plans 01-03 and 01-04 are sequential within wave 3 — 01-04 depends on this plan's UI primitives.
Output: 9 files — Tailwind config, root layout, 4 primitive UI components, LoginForm, login page route, verify page route.
</objective>

<execution_context>
@/Users/robertlevin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robertlevin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-foundation/01-02-SUMMARY.md

<design_tokens>
<!-- CIS brand tokens — apply these throughout. Source of truth: design-system/cis-deal-room/MASTER.md -->
<!-- Check design-system/cis-deal-room/pages/ for per-page overrides before implementing any page -->

Colors (define in globals.css @theme):
  --color-brand: #E10600          /* Primary accent — CTA buttons, active states, links */
  --color-brand-hover: #C50500    /* Hover state for brand red */
  --color-bg-base: #000000        /* Page background */
  --color-bg-surface: #0D0D0D     /* App shell / sidebar */
  --color-bg-elevated: #141414    /* Cards, modals, panels */
  --color-bg-input: #1F1F1F       /* Input fields, hover states */
  --color-border: #2A2A2A         /* Dividers, borders */
  --color-text-primary: #FFFFFF
  --color-text-secondary: #888888
  --color-text-muted: #555555

Fonts (define in globals.css @theme):
  --font-sans: 'DM Sans'          /* All UI text */
  --font-mono: 'JetBrains Mono'   /* File sizes, timestamps, IDs — data values only */
</design_tokens>

<interfaces>
<!-- From Plan 01-02 DAL outputs. Use directly. -->

From src/lib/dal/index.ts:
```typescript
export const verifySession: () => Promise<Session | null>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Brand configuration, primitive UI components, and auth screens</name>
  <files>
    cis-deal-room/src/app/globals.css,
    cis-deal-room/src/app/layout.tsx,
    cis-deal-room/src/components/ui/Button.tsx,
    cis-deal-room/src/components/ui/Input.tsx,
    cis-deal-room/src/components/ui/Modal.tsx,
    cis-deal-room/src/components/ui/Badge.tsx,
    cis-deal-room/src/components/auth/LoginForm.tsx,
    cis-deal-room/src/app/(auth)/login/page.tsx,
    cis-deal-room/src/app/auth/verify/page.tsx
  </files>
  <behavior>
    - LoginForm has two states: 'input' (email field + submit button) and 'sent' (confirmation message showing the specific email address)
    - State transition: input → sent happens on successful API response (no page navigation)
    - 'sent' state shows 'Check your email' heading + 'We sent a link to {email}' subtext + 'Resend email' button (immediately available, no cooldown)
    - /auth/verify page reads ?error= query param: 'expired' renders "This link has expired", 'used' renders "This link has already been used", fallback renders "Invalid link" — each with a 'Request new link' button that links to /login
    - Badge component variants: 'status' badges for workspace lifecycle (engagement, active_dd, ioi_stage, closing, closed, archived) — map enum values to human labels
    - Button primitive: variant='primary' uses bg-brand text-white, variant='ghost' uses transparent with border
    - No page navigation on login form submit — inline state machine only (per CONTEXT.md decision)
    - No emoji anywhere — lucide-react for any icons used in auth UI (e.g., Mail icon on confirm state)
  </behavior>
  <action>
    Read design-system/cis-deal-room/MASTER.md and design-system/cis-deal-room/pages/ before implementing anything. The design system is authoritative for all styling decisions.

    Configure globals.css with Tailwind v4 @theme block (NOT tailwind.config.ts — see RESEARCH.md Pitfall 6). Import DM Sans and JetBrains Mono from Google Fonts in layout.tsx using Next.js font optimization (next/font/google). Apply font-sans to body.

    Create UI primitives (Button, Input, Modal, Badge) in src/components/ui/. Use clsx + tailwind-merge for class composition. All components accept className prop for overrides.

    Modal: fixed inset-0 overlay (bg-black/60 backdrop), centered card (bg-elevated, rounded-lg, max-w-lg), close on backdrop click and Escape key.

    Create LoginForm.tsx as a 'use client' component using useState for the two-state machine. On submit: POST to /api/auth/send, on success transition to 'sent' state showing the email address. The email input fades out (use opacity-0 transition), confirmation appears in place.

    /auth/verify page: reads searchParams.error — show appropriate error message. 'Request new link' button routes to /login.

    Update the test stubs from Plan 01-01 (LoginForm.test.tsx, verify/page.test.tsx) to pass GREEN:
    - LoginForm.test.tsx: render in 'input' state, submit transitions to 'sent' state showing email
    - verify/page.test.tsx: render with error='expired' shows expiry message; error='used' shows used message
  </action>
  <verify>
    <automated>cd cis-deal-room && npx vitest run src/components/auth/ src/app/auth/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - globals.css has @theme block with all CIS brand color and font tokens
    - LoginForm has two-state machine: 'input' to 'sent' with email address shown, no page nav
    - /auth/verify page shows distinct messages for error=expired vs error=used
    - UI primitives (Button, Input, Modal, Badge) exist in src/components/ui/
    - Auth component tests pass GREEN
    - No emoji in any UI — lucide-react only
  </done>
</task>

</tasks>

<verification>
- `cd cis-deal-room && npx vitest run src/components/auth/ src/app/auth/ --reporter=verbose` — all auth component tests GREEN
- `cd cis-deal-room && npx tsc --noEmit` exits 0
- globals.css contains @theme block with --color-brand, --font-sans, --font-mono tokens
- LoginForm.tsx has useState for 'input' or 'sent' state
- No emoji in any .tsx file
</verification>

<success_criteria>
- globals.css has @theme block with all CIS brand tokens (colors + fonts)
- LoginForm two-state machine works (input → sent, no page nav)
- /auth/verify shows distinct error messages for expired vs used
- UI primitives (Button, Input, Modal, Badge) are TypeScript-clean and accept className prop
- Auth component test stubs pass GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-foundation/01-03-SUMMARY.md` with:
- Brand tokens applied (list of CSS custom properties in @theme)
- UI primitives created (name + variants)
- Auth screen behaviors confirmed (LoginForm states, verify page error messages)
- Any deviations from design spec
</output>
