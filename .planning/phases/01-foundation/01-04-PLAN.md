---
phase: 01-foundation
plan: "04"
type: execute
wave: 4
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified:
  - cis-deal-room/src/app/(app)/layout.tsx
  - cis-deal-room/src/app/(app)/deals/page.tsx
  - cis-deal-room/src/app/(app)/workspace/[workspaceId]/page.tsx
  - cis-deal-room/src/components/deals/DealList.tsx
  - cis-deal-room/src/components/deals/NewDealModal.tsx
  - cis-deal-room/src/components/workspace/WorkspaceShell.tsx
  - cis-deal-room/src/components/workspace/FolderSidebar.tsx
  - cis-deal-room/src/components/workspace/DealOverview.tsx
  - cis-deal-room/src/components/workspace/RightPanel.tsx
autonomous: false
requirements:
  - UI-07
  - WORK-01
  - WORK-02
  - WORK-03
  - FOLD-01
  - FOLD-02
  - FOLD-03

must_haves:
  truths:
    - "Deal list renders workspaces from the API; admin sees 'New Deal Room' button; non-admin does not"
    - "NewDealModal is a centered overlay with Codename, Client Name, CIS Advisory Side (radio), Initial Status (dropdown) fields"
    - "After workspace creation user is navigated immediately into the workspace"
    - "Three-panel workspace layout renders: FolderSidebar (240px), center file list area (flex-1), RightPanel (320px)"
    - "Workspace default state shows DealOverview (no folder selected) with deal name, status badge, advisory side, creation date, per-folder file counts"
    - "Status badge in workspace header is clickable for admin to change status"
    - "No emoji anywhere in the UI — lucide-react icons for all iconography"
  artifacts:
    - path: "cis-deal-room/src/components/workspace/WorkspaceShell.tsx"
      provides: "Three-panel layout container"
      exports: ["WorkspaceShell"]
    - path: "cis-deal-room/src/components/workspace/DealOverview.tsx"
      provides: "No-folder-selected center panel with workspace summary"
      exports: ["DealOverview"]
    - path: "cis-deal-room/src/components/deals/NewDealModal.tsx"
      provides: "Create workspace modal overlay"
      exports: ["NewDealModal"]
    - path: "cis-deal-room/src/components/deals/DealList.tsx"
      provides: "Workspace list with conditional admin controls"
      exports: ["DealList"]
    - path: "cis-deal-room/src/components/workspace/FolderSidebar.tsx"
      provides: "Folder navigation with admin rename/delete"
      exports: ["FolderSidebar"]
  key_links:
    - from: "cis-deal-room/src/components/deals/NewDealModal.tsx"
      to: "cis-deal-room/src/app/api/workspaces/route.ts"
      via: "fetch POST /api/workspaces on form submit, then router.push to workspace"
      pattern: "fetch.*api/workspaces"
    - from: "cis-deal-room/src/app/(app)/workspace/[workspaceId]/page.tsx"
      to: "cis-deal-room/src/lib/dal/workspaces.ts"
      via: "Server Component calling getWorkspace() and getFoldersForWorkspace()"
      pattern: "getWorkspace|getFoldersForWorkspace"

user_setup:
  - service: neon
    why: "PostgreSQL database for the application"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Dashboard -> Project -> Connection string (use pooled connection)"
    dashboard_config:
      - task: "Create a new Neon project named 'cis-deal-room'"
        location: "console.neon.tech -> New Project"
  - service: upstash
    why: "Redis for rate limiting auth endpoints (AUTH-06)"
    env_vars:
      - name: UPSTASH_REDIS_REST_URL
        source: "Upstash Console -> Database -> REST API -> UPSTASH_REDIS_REST_URL"
      - name: UPSTASH_REDIS_REST_TOKEN
        source: "Upstash Console -> Database -> REST API -> UPSTASH_REDIS_REST_TOKEN"
    dashboard_config:
      - task: "Create a Redis database named 'cis-deal-room-ratelimit'"
        location: "console.upstash.com -> Create Database"
  - service: resend
    why: "Transactional email for magic links (AUTH-01)"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys -> Create API Key"
    dashboard_config:
      - task: "Verify sending domain or use Resend's onboarding.resend.dev for testing"
        location: "resend.com/domains"
  - service: iron-session
    why: "Encrypted session cookie"
    env_vars:
      - name: IRON_SESSION_SECRET
        source: "Generate locally: node -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\""
  - service: next
    why: "Public app URL for magic link generation"
    env_vars:
      - name: NEXT_PUBLIC_APP_URL
        source: "Set to http://localhost:3000 for local dev; update to production URL on deploy"
---

<objective>
Build the workspace shell, deal list, and workspace creation modal — the application screens that make the Phase 1 user journey complete end-to-end. Closes with a human-verify checkpoint confirming the full journey works in a live browser.

Purpose: This plan delivers the product-facing layer on top of the backend (Plan 01-02) and UI primitives (Plan 01-03). After this plan completes, a user can log in via magic link, create a deal workspace, and navigate the three-panel workspace.
Output: 9 files — app layout, deals page, workspace page, 4 workspace components, 2 deal components. Followed by a blocking checkpoint.
</objective>

<execution_context>
@/Users/robertlevin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robertlevin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-foundation/01-03-SUMMARY.md

<design_tokens>
<!-- CIS brand tokens — already defined in globals.css by Plan 01-03. Use via Tailwind classes. -->
<!-- Check design-system/cis-deal-room/pages/ for per-page overrides before implementing any page -->

Layout:
  Folder sidebar: width 240px, fixed left
  File list: flex-1, main content area
  Right panel: width 320px, fixed right (RESEARCH.md note: MASTER.md says 320px, not 280px)

Apply throughout: bg-[#0D0D0D] for surface, bg-[#141414] for panels, #E10600 for active/accent, text-white primary, font-mono for timestamps and file sizes.
</design_tokens>

<interfaces>
<!-- From Plan 01-02 DAL outputs. Use directly. -->

From src/lib/dal/workspaces.ts:
```typescript
export async function getWorkspacesForUser(): Promise<Workspace[]>;
export async function getWorkspace(id: string): Promise<Workspace | null>;
export async function createWorkspace(input: {
  name: string;
  clientName: string;
  cisAdvisorySide: 'buyer_side' | 'seller_side';
  status: WorkspaceStatus;
  createdBy: string;
}): Promise<Workspace>;
export async function updateWorkspaceStatus(workspaceId: string, status: WorkspaceStatus): Promise<void>;
```

From src/lib/dal/folders.ts:
```typescript
export async function getFoldersForWorkspace(workspaceId: string): Promise<Folder[]>;
export async function createFolder(workspaceId: string, name: string): Promise<Folder>;
export async function renameFolder(folderId: string, name: string): Promise<void>;
export async function deleteFolder(folderId: string): Promise<void>;
```

From src/lib/dal/index.ts:
```typescript
export const verifySession: () => Promise<Session | null>;
```

From src/components/ui/ (Plan 01-03):
```typescript
export function Button(props: { variant: 'primary' | 'ghost'; className?: string; ... }): JSX.Element;
export function Modal(props: { open: boolean; onClose: () => void; children: React.ReactNode }): JSX.Element;
export function Badge(props: { status: WorkspaceStatus; className?: string }): JSX.Element;
export function Input(props: React.InputHTMLAttributes<HTMLInputElement> & { className?: string }): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deal list, NewDealModal, workspace shell with folder sidebar and deal overview</name>
  <files>
    cis-deal-room/src/app/(app)/layout.tsx,
    cis-deal-room/src/app/(app)/deals/page.tsx,
    cis-deal-room/src/app/(app)/workspace/[workspaceId]/page.tsx,
    cis-deal-room/src/components/deals/DealList.tsx,
    cis-deal-room/src/components/deals/NewDealModal.tsx,
    cis-deal-room/src/components/workspace/WorkspaceShell.tsx,
    cis-deal-room/src/components/workspace/FolderSidebar.tsx,
    cis-deal-room/src/components/workspace/DealOverview.tsx,
    cis-deal-room/src/components/workspace/RightPanel.tsx
  </files>
  <action>
    Create src/app/(app)/layout.tsx — auth-gated layout. This is a Server Component that calls verifySession(). If null, redirect to /login. Renders the persistent app shell (no navigation chrome needed in Phase 1 — just the auth gate).

    Create src/app/(app)/deals/page.tsx — Server Component. Calls getWorkspacesForUser() and verifySession(). Renders DealList with workspace data. Passes isAdmin flag to DealList to conditionally render 'New Deal Room' button.

    Create src/components/deals/DealList.tsx — 'use client' component. Props: workspaces[], isAdmin. Renders a list of workspace rows (simple list in Phase 1 — card layout is Phase 4). Admin sees 'New Deal Room' button that opens NewDealModal. On workspace click, routes to /workspace/[id].

    Create src/components/deals/NewDealModal.tsx — 'use client' component. Uses Modal primitive from Plan 01-03. Four fields with zod validation:
    1. Deal Codename (text input, required)
    2. Client Name (text input, required — label: "Client Name (admin-visible only)")
    3. CIS Advisory Side (radio group: "Buyer-side" / "Seller-side", required, cannot be deselected)
    4. Initial Status (dropdown select with all 6 status options: Engagement, Active DD, IOI Stage, Closing, Closed, Archived)
    On submit: POST to /api/workspaces with { name, clientName, cisAdvisorySide, status }. On success: close modal + router.push('/workspace/' + workspace.id). Use router from 'next/navigation'.

    Create src/app/(app)/workspace/[workspaceId]/page.tsx — Server Component. Awaits params: `const { workspaceId } = await params` (Next.js 15 async params). Calls getWorkspace(workspaceId) and getFoldersForWorkspace(workspaceId). Passes data to WorkspaceShell.

    Create src/components/workspace/WorkspaceShell.tsx — 'use client' component. Three-panel flex layout:
    - FolderSidebar: w-[240px] shrink-0, fixed left
    - Main area: flex-1 min-w-0 (renders DealOverview when no folder selected via selectedFolderId state)
    - RightPanel: w-[320px] shrink-0, fixed right
    Header bar across top: placeholder logo slot (clearly marked with a comment "// TODO: Replace with CIS Partners logo asset"), deal codename, status badge (clickable for admin opens status dropdown), workspace metadata.

    Create src/components/workspace/FolderSidebar.tsx. Props: folders[], workspaceId, selectedFolderId, onFolderSelect, isAdmin. Lists folders with lucide-react Folder icon (use FolderOpen for selected). Folder items are clickable. Admin gets inline rename (click on folder name activates input) and delete (Trash2 icon) per FOLD-02. Plus icon button at bottom for admin to add folder.

    Create src/components/workspace/DealOverview.tsx. Shows when no folder is selected (selectedFolderId = null). Displays:
    - Deal name (large heading)
    - Status badge
    - CIS advisory side (e.g., "Buyer-side Advisory")
    - Creation date formatted with font-mono (JetBrains Mono)
    - Summary grid: each of the 8 folders as a card with folder name and file count (0 in Phase 1 — files come in Phase 2)

    Create src/components/workspace/RightPanel.tsx. Two tabs: 'Activity' and 'Participants'. Activity tab is default on workspace entry (per CONTEXT.md). In Phase 1, both tabs show a placeholder state (activity feed UI is Phase 4; participant management is Phase 3). Tabs use the CIS brand accent on active state.

    Status badge click (admin only): implement with a dropdown using a Popover-style pattern (absolute positioned div below the badge). Options are the 6 workspace status values. On select, call PATCH /api/workspaces/[id]/status then optimistically update local state.

    Do NOT port inline styles from cis-deal-portal-prototype.jsx — rebuild in Tailwind classes only.
    Do NOT render emoji anywhere — lucide-react for all icons (Folder, FolderOpen, Plus, Trash2, ChevronDown, etc.).
  </action>
  <verify>
    <automated>cd cis-deal-room && npx tsc --noEmit 2>&1 | grep -E "error TS" | head -20</automated>
  </verify>
  <done>
    - /deals page renders workspace list with conditional 'New Deal Room' button (admin only)
    - NewDealModal has all 4 required fields (codename, client name, advisory side radio, status dropdown)
    - After workspace creation, user is routed to the new workspace immediately
    - Workspace page renders three-panel layout: FolderSidebar (240px) + main (flex-1) + RightPanel (320px)
    - Default workspace state shows DealOverview with folder count grid (all 0 in Phase 1)
    - Status badge is clickable for admin; non-admin sees static badge
    - FolderSidebar: rename and delete available to admin (FOLD-02)
    - Logo slot in header has a clear TODO comment marking the placeholder
    - Zero emoji in any component — all icons from lucide-react
    - `npx tsc --noEmit` exits 0
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify complete Phase 1 user journey end-to-end</name>
  <files>none (verification only)</files>
  <what-built>
    Complete Phase 1 user journey: login screen, magic link auth, deal list, workspace creation, three-panel workspace shell with folder sidebar and deal overview.

    Auth infrastructure: magic link generation/hashing, database sessions, rate limiting.
    Data layer: workspace + folder CRUD with activity logging.
    Foundation utilities: requireDealAccess / requireFolderAccess stubs (Phase 3), getS3Client stub (Phase 2).
    UI: login screen with inline state machine, deal list, workspace creation modal, three-panel workspace.
  </what-built>
  <how-to-verify>
    Before running the app, set up environment variables (see user_setup in frontmatter):
    1. Create a .env.local file in cis-deal-room/ with DATABASE_URL, UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN, IRON_SESSION_SECRET, RESEND_API_KEY, NEXT_PUBLIC_APP_URL=http://localhost:3000
    2. Run database migrations: `cd cis-deal-room && npx drizzle-kit push`
    3. Start the dev server: `cd cis-deal-room && npm run dev`
    4. Visit http://localhost:3000

    Test the full journey:
    [ ] Login screen: CIS Partners branding visible, email input works, form submits to 'Check your email' state without page reload
    [ ] The confirmation shows the specific email address you entered
    [ ] 'Resend email' button is immediately available (no cooldown shown)
    [ ] Magic link in your email works — clicking it lands you at /deals
    [ ] /deals shows workspace list (empty for new install) with 'New Deal Room' button
    [ ] New Deal Room modal opens centered over the page with all 4 fields
    [ ] CIS Advisory Side shows as radio buttons (not dropdown)
    [ ] Creating a workspace immediately navigates to the workspace
    [ ] Workspace shows three panels: folder sidebar (left), overview (center), right panel
    [ ] Folder sidebar lists all 8 default folders with Folder icons (no emoji)
    [ ] DealOverview shows deal name, status badge, advisory side, creation date, folder count grid (all 0)
    [ ] Clicking a folder in the sidebar selects it (center panel shows empty file list — fine for Phase 1)
    [ ] Status badge in header is clickable for admin: dropdown opens, selecting a status updates it
    [ ] Brand: dark background (#0D0D0D), red accent (#E10600) on CTAs, DM Sans font throughout
    [ ] JetBrains Mono used for timestamps and data values
    [ ] No emoji visible anywhere in the app
    [ ] Test magic link expiry: visit /auth/verify?error=expired — distinct error message + 'Request new link' button
    [ ] Test used link: visit /auth/verify?error=used — distinct error message + 'Request new link' button
  </how-to-verify>
  <verify>
    <automated>cd cis-deal-room && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>All Phase 1 success criteria confirmed: full auth flow works, workspace creation works, three-panel shell renders with correct branding, no emoji anywhere in the app, all Vitest tests pass GREEN.</done>
  <resume-signal>Type "approved" to proceed to Phase 2 planning. Or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
- `cd cis-deal-room && npx vitest run --reporter=verbose` — all test stubs GREEN
- `cd cis-deal-room && npx tsc --noEmit` exits 0
- WorkspaceShell.tsx renders three-panel flex layout with correct widths (240px, flex-1, 320px)
- NewDealModal.tsx has radio inputs for CIS Advisory Side (not a select dropdown)
- No emoji in any .tsx file
</verification>

<success_criteria>
Phase 1 is complete when:
1. User can enter email on /login, receive magic link, click it, and land on /deals — link expires after 10 min and is single-use
2. User can create a new deal workspace (modal with all 4 fields) and immediately enter it
3. Three-panel workspace renders with all 8 default folders, DealOverview as default center, Activity tab as default right panel
4. Admin can rename and delete folders; admin can click status badge to change workspace status
5. /auth/verify shows distinct error messages for expired vs already-used links
6. CIS brand applied: #E10600 accent, dark surfaces, DM Sans + JetBrains Mono fonts, lucide-react icons only
7. All Vitest tests pass GREEN; TypeScript compiles with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-foundation/01-04-SUMMARY.md` with:
- Components built (name + purpose)
- Design system tokens applied
- User journey walkthrough (what screens exist and how they connect)
- Any deviations from design spec
- Checkpoint outcome (approved / issues found)
</output>
